import {addContloScript, apiKeyValidator, urlValidator} from './utils'

let initialized = false
let url = ''
let headers = {}

export function featureBFunction() {
    return "This is Feature B!";
}

export function featureAFunction() {
    return "This is Feature A!";
}

export async function init(apiKeyX, urls) {
    const isValidKey = await apiKeyValidator(apiKeyX);
    const isValidUrl = await urlValidator(urls);
    if(!isValidKey && !isValidUrl)
        return
    addContloScript(apiKeyX);
    initialized = true;
    // const currentTime = new Date();
    // const browserName = navigator.userAgent;
    // const browserVersion = navigator.appVersion;
    // const currentPage = window.location.href;
    // const osType = navigator.platform;
}

export async function sendEvent(eventName, parameters){

}
